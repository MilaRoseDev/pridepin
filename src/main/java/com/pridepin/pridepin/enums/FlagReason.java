package com.pridepin.pridepin.enums;

/** Why a community member is flagging a location for admin review. */
public enum FlagReason {
    NO_LONGER_SAFE,
    HOSTILE_ENVIRONMENT,
    INCORRECT_INFO,
    PERMANENTLY_CLOSED,
    WRONG_LOCATION,
    DUPLICATE,
    OTHER
}
